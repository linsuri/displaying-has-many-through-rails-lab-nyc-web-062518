<h1>All Doctors</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name, doctor_path(doctor) %> | number of patients: <%= doctor.patients.count %>
    </li>
  <% end %>
</ul>
